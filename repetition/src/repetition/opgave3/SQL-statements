SQL-statements

Person-tabel:
"CREATE TABLE person(id bigint generated by default as IDENTITY(start with 1) UNIQUE+, name varchar(80) NOT NULL, email varchar(255) UNIQUE, birthdate date NOT NULL)"

Employment-tabel:
"CREATE TABLE employment( id bigint, company varchar (50) NOT NULL, employmentdate date NOT NULL, person bigint NOT NULL, FOREIGN KEY (person) references person(id) on delete cascade)"

INSERT INTO "PUBLIC"."PERSON"
( "NAME", "EMAIL", "BIRTHDATE" )
VALUES ( 'Alina', 'ali@gmail.com', '2014-03-08')

